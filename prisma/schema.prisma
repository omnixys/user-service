generator client {
  provider = "prisma-client"
  output   = "../src/prisma/generated"
}

datasource db {
  provider = "postgresql"
}

/// ------------------------------------------------------------
/// USER SERVICE SCHEMA
/// ------------------------------------------------------------
model User {
  id       String @id @default(uuid()) @map("id") @db.Uuid
  username String @unique @map("username")

  userType     UserType      @map("user_type")
  status       PersonStatus  @default(ACTIVE) @map("status")
  customer     Customer?
  employee     Employee?
  personalInfo PersonalInfo?

  contacts Contact[]

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at")

  @@index([status])
  @@index([userType])
  @@index([status, userType])
  @@index([createdAt])
  @@map("user")
}

/// ------------------------------------------------------------
/// PERSON SERVICE SCHEMA
/// ------------------------------------------------------------
model PersonalInfo {
  id    String @id @map("id") @db.Uuid
  email String @unique @map("email")

  firstName     String             @map("first_name")
  lastName      String             @map("last_name")
  birthDate     DateTime?          @map("birth_date")
  gender        GenderType?        @map("gender")
  maritalStatus MaritalStatusType? @map("marital_status")

  phoneNumbers PhoneNumber[]

  user User @relation(fields: [id], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at")

  @@index([lastName])
  @@index([firstName, lastName])
  @@map("personal_info")
}

/// ------------------------------------------------------------
/// CUSTOMER (FACHLICHE ERWEITERUNG)
/// ------------------------------------------------------------

model Customer {
  id String @id @map("id") @db.Uuid

  subscribed Boolean    @default(false)
  state      StatusType @map("customer_state")

  contactOptions ContactOptionsType[] @map("contact_options")

  user User @relation(fields: [id], references: [id], onDelete: Cascade)

  createdAt         DateTime           @default(now()) @map("created_at")
  updatedAt         DateTime           @default(now()) @updatedAt @map("updated_at")
  customerInterests CustomerInterest[]

  @@index([state])
  @@index([subscribed])
  @@map("customer")
}

/// ------------------------------------------------------------
/// EMPLOYEE (FACHLICHE ERWEITERUNG)
/// ------------------------------------------------------------

model Employee {
  id String @id @map("id") @db.Uuid

  department String?   @map("department")
  position   String?   @map("position")
  role       String?   @map("role")
  salary     Float?    @map("salary")
  hireDate   DateTime? @map("hire_date")
  isExternal Boolean   @default(false) @map("is_external")

  user User @relation(fields: [id], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at")

  @@index([department])
  @@index([role])
  @@index([isExternal])
  @@map("employee")
}

model PhoneNumber {
  id     String @id @default(uuid()) @map("id") @db.Uuid
  infoId String @map("info_id") @db.Uuid

  number      String          @map("number")
  type        PhoneNumberType @map("type")
  label       String?         @map("label")
  isPrimary   Boolean         @default(false) @map("is_primary")
  countryCode String          @map("country_code")

  user PersonalInfo @relation(fields: [infoId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at")

  @@index([countryCode, number])
  @@index([infoId])
  @@index([infoId, isPrimary])
  @@map("phone_number")
}

/// ------------------------------------------------------------
/// CONTACTS (PERSON ↔ PERSON)
/// ------------------------------------------------------------

model Contact {
  id        String @id @default(uuid()) @map("id") @db.Uuid
  userId    String @map("user_id") @db.Uuid
  contactId String @map("contact_id") @db.Uuid

  relationship    RelationshipType @map("relationship")
  withdrawalLimit Int              @default(0) @map("withdrawal_limit")
  emergency       Boolean          @default(false) @map("emergency")

  startDate DateTime? @map("start_date")
  endDate   DateTime? @map("end_date")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at")

  @@unique([userId, contactId])
  @@index([userId])
  @@index([emergency])
  @@map("contact")
}

model InterestCategory {
  id   String              @id @default(uuid()) @db.Uuid
  key  InterestCategoryKey @unique // e.g. FINANCE, LIFESTYLE
  name String
  icon String?

  description String?
  interests   Interest[]

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at")

  @@map("interest_category")
}

model Interest {
  id         String      @id @default(uuid()) @db.Uuid
  key        InterestKey @unique
  name       String
  categoryId String      @map("category_id") @db.Uuid
  icon       String?

  category InterestCategory @relation(fields: [categoryId], references: [id], onDelete: Restrict)

  customers CustomerInterest[]

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at")

  @@index([categoryId])
  @@map("interest")
}

model CustomerInterest {
  id         String @id @default(uuid()) @db.Uuid
  customerId String @map("customer_id") @db.Uuid
  interestId String @map("interest_id") @db.Uuid

  // Optional future attributes:
  level     Int? // e.g. intensity 1–5
  isPrimary Boolean @default(false) @map("is_primary")

  customer Customer @relation(fields: [customerId], references: [id], onDelete: Cascade)
  interest Interest @relation(fields: [interestId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([customerId, interestId], name: "customer_id_interest_id")
  @@index([interestId])
  @@map("customer_interest")
}

/// ------------------------------------------------------------
/// ENUMS
/// ------------------------------------------------------------
enum UserType {
  CUSTOMER
  EMPLOYEE
  GUEST

  @@map("user_type")
}

enum PhoneNumberType {
  WHATSAPP
  MOBILE
  PRIVATE
  WORK
  HOME
  OTHER

  @@map("phone_number_type")
}

enum PersonStatus {
  ACTIVE
  DISABLED
  DELETED
  INACTIVE
  BLOCKED
  CLOSED

  @@map("person_status")
}

enum GenderType {
  MALE
  FEMALE
  DIVERSE
  UNKNOWN

  @@map("gender_type")
}

enum RelationshipType {
  FAMILY
  FRIEND
  PARTNER
  COLLEAGUE
  OTHER
  BUSINESS_PARTNER
  RELATIVE
  PARENT
  SIBLING
  CHILD
  COUSIN

  @@map("relationship_type")
}

enum MaritalStatusType {
  SINGLE
  MARRIED
  DIVORCED
  WIDOWED

  @@map("marital_status_type")
}

enum StatusType {
  ACTIVE
  INACTIVE
  BLOCKED
  PENDING
  SUSPENDED
  CLOSED

  @@map("status_type")
}

enum InterestKey {
  BANK_PRODUCTS_AND_SERVICES
  TECHNOLOGY_AND_INNOVATION
  FINANCIAL_EDUCATION_AND_COUNSELING
  SUSTAINABLE_FINANCE
  INVESTMENTS
  SAVING_AND_FINANCE
  CREDIT_AND_DEBT
  REAL_ESTATE
  INSURANCE

  // Lifestyle
  TRAVEL

  // Music
  CLASSIC
  ROCK
  HIPHOP
  RAP

  // Sports
  FOOTBALL
  SOCCER
  RUGBY
  BASKETBALL

  @@map("interest_key")
}

enum InterestCategoryKey {
  SPORTS
  MUSIC
  FINANCE
  REAL_ASSETS
  TECHNOLOGY
  LIFESTYLE

  @@map("interest_category_key")
}

enum ContactOptionsType {
  EMAIL
  PHONE
  SMS
  WHATSAPP
  LETTER

  @@map("contact_options_type")
}
